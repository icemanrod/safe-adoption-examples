# vendor/

The compiled engine lives here in the published package.

In this synthetic example the binary is intentionally NOT committed — shipping a
real blob would make this repository something people should not clone. The
decision packet records its absence as an omission, which is the behaviour being
demonstrated: material that was never read is recorded as never read.
